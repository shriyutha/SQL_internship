The Clauses used in this file like 
SELECT: used to display the columns, 
WHERE: used to filter the rows, 
ORDER BY: used to sort the results,
FROM: specifies which table to use, 
DISTINCT: to remove duplicate values,
LIKE/ILIKE: for specific matching values, 
AND/OR: boolean conditions,
IS NULL/IS NOT NULL: checking missing values, 
AS: to rename tables/columns,
IN: check multiple values from range, 
BETWEEN: filtering the range.
COALESCE: fills missing values
