# Setting up SQL Environment & First Queries.

### Class_1:

Tools: PostgreSQL

1.Installed PostgreSQL.

2.Created a new database named intern_training_db

3.Created a table called students with columns like id, firstname, lastname, email, grade, and age.

5.Inserted 5 records using INSERT INTO statements.

6.Retrieve and execute queries multiple times to understand execution flow and result sets.


### Class_2:

Constrain and the valid data types are very important for sql queries because,they ensure data integrity by not allowing invalid data, duplications and ,issing data in the table.
In this data, have used SERIAL - for sequences, 
PRIMARY KEY - identify unique values in each row, 
NOT NULL - ensures no missing values, 
UNIQUE - ensures no duplicate values, 
DATE - for valid date format, 
VARCHAR(100) - text within 100 letters, 
INT - numeric values, 
FLOAT - decimal values
